import styles from "./HomePage.module.css";
import Navigation from "../Navigation";

const HomePage = () => {
	return (
		<div className={styles.banner}>
			<div className={styles.inner_middle_banner}></div>
		</div>
	);
};

export default HomePage;
